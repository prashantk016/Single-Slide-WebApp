import { BoxHostDirective } from './box-host.directive';

describe('BoxHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxHostDirective();
    expect(directive).toBeTruthy();
  });
});
