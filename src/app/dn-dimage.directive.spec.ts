import { DnDimageDirective } from './dn-dimage.directive';

describe('DnDimageDirective', () => {
  it('should create an instance', () => {
    const directive = new DnDimageDirective();
    expect(directive).toBeTruthy();
  });
});
